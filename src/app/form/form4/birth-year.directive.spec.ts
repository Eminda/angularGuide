import { BirthYearDirective } from './birth-year.directive';

describe('BirthYearDirective', () => {
  it('should create an instance', () => {
    const directive = new BirthYearDirective();
    expect(directive).toBeTruthy();
  });
});
